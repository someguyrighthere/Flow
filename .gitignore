# Dependencies folder
# This is the most important line. It prevents your local, OS-specific
# node_modules from being uploaded. The server will install its own.
node_modules/

# Environment variables file
# This file often contains secret keys and should never be public.
.env

# Database file
# You don't want to overwrite the server's database with your local one.
*.db

# Build artifacts
# These files are generated by the build command, so we don't need to upload them.
# The server will create its own versions.
/js/app.build.js
/js/app.min.js
/css/style.min.css
/css/theme.min.css
